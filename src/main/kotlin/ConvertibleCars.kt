

/*If the superclass constructor includes parameters, you must pass values for these
parameters when you call the constructor. As an example, suppose you have a
Car class that has two parameters in its constructor named make and model:
*/
class ConvertibleCars(make_param:String, model:String):Car(make_param,model) {


}